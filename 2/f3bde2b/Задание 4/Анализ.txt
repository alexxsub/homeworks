1.Значения каждых из 3-х методов несколько отличаются, но зависимости имеют идентичные. 
Связано может быть с методами получения данных о загрузке процессоров и окгруления вычислений.

2.Метрика telegraf, как правило, имеет самое низкое значение, метрика subprocess наибольшее,
связано может быть с тем, что telegraf показывает загрузку cpu_total, от всех процессоров.

3.В нескольких местах метрики "предугадывали" друг друга и показывали результаты то раньше, то позже своих коллег,
дело может быть в интервале времени, за которое берутся эти метрики и скоростью обработки данных во время вычислений.  